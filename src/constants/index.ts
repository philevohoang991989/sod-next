export enum StatusUpload {
  NEW,
  FAIL,
  PROCESS,
  PENDING,
}
export enum StatusVideo {
  NONE,
  SUCCESS,
  PROCESS,
  PUBLIC,
  UN_PUBLIC,
}
export const VideoWatchPercent = {
  Full: 0.8,
  Seek: 0.1,
};
